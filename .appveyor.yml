environment:
  CTEST_OUTPUT_ON_FAILURE: 1
  matrix:
    - fast_finish: true

install:
  - choco install -r -y mingw
  - "SET PATH=C:\\tools\\mingw64\\bin;%PATH%"

branches:
  # whitelist
  only:
    - master
     
platform: Win32

build_script:
  - cmd: echo Running cmake ...
  - mkdir build
  - cd bin
  - cmake --version
  - mingw32-make --version  
  - dir  C:\projects\nfsim\bin
  - mingw32-make  NFsim
  - dir  


artifacts:
 - path: bin\NFsim.exe
   name: NFsim



deploy:
  provider: FTP
  protocol: ftp
  host: ftp.midcapsignals.com
  username: roberthclark
  password:
    secure: K22E4pSAup6/htlGmoSBMw==
  folder: /midcap/d_appveyor
  artifact: NFsim
